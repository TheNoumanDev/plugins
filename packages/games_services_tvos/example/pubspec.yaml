name: games_services_example
description: Demonstrates how to use the games_services plugin on Apple TV.
publish_to: none

dependencies:
  games_services: ^5.3.0
  games_services_tvos:
    path: ../
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"
